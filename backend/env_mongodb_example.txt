# MongoDB Configuration
# Copy file này thành .env và thay thế thông tin của bạn

# Cho MongoDB Atlas (Cloud):
# Dán connection string ở đây (URL-encode mật khẩu nếu có ký tự đặc biệt)
MONGODB_URL=
MONGODB_DATABASE=elearning_platform
# MONGODB_AUTH_SOURCE=admin

# Cho MongoDB Local:
# Ví dụ MongoDB Local (tham khảo):
# MONGODB_URL=mongodb://localhost:27017
# MONGODB_DATABASE=elearning_platform
# MONGODB_AUTH_SOURCE=admin

# Security
SECRET_KEY=your-secret-key-change-this-in-production-12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# Environment
ENVIRONMENT=development
LOG_LEVEL=INFO
